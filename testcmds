git pull
sudo cp -r ~/myapp/. /var/www/myphpapp/
sudo systemctl restart myphpapp

-- 

for i in {1..20}; do
  curl -s -o /dev/null \
    -H "Connection: close" \
    -w "w:%header{x-worker-id} %{time_starttransfer}s\n" \
    http://127.0.0.1:8080/ &
done
wait

---
sudo journalctl -u myphpapp -n 20


---
# summary
watch -n 1 'ps aux | grep "server.php" | grep -v grep'

#with htop
Open htop after installing:
sudo apt install htop -y
htop

Press F4 → type php to filter only PHP workers
Each worker's CPU, RAM, PID shown separately
Press F2 → Display options → enable Tree view to see parent-child relationship
